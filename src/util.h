#pragma once

#include <cstddef>
#include <string>
#include <vector>

namespace cryptopals {
// get the score for a string; lower is better.
float score_string(const std::string &s);
}  // namespace cryptopals
